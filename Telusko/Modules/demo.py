from Calc import add, sub, multi, div

a = 9
b = 7

print(add(a,b))
print(sub(a,b))
print(multi(a,b))

# Python modules is a Python code or a file that contains code that you will import from another module.
# Technically any Python file is a module. But, We refer to something as a module, usually when we're importing it from another script.

# Package is a directory that contains python modules.